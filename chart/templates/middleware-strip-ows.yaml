apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-ows
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "qgis.labels" . | nindent 4 }}
spec:
  stripPrefix:
    prefixes:
      - /ows
